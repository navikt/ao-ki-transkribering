name: Build and deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-api:
    name: Build and push API image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    outputs:
      image: ${{ steps.build.outputs.image }}
    steps:
      - uses: actions/checkout@v4

      - name: Build and push
        uses: nais/docker-build-push@v0
        id: build
        with:
          team: ao-ki-taskforce
          identity_provider: ${{ secrets.NAIS_WORKLOAD_IDENTITY_PROVIDER }}
          project_id: ${{ vars.NAIS_MANAGEMENT_PROJECT_ID }}
          # Default target: runtime (CPU, full ML stack for real-time WebSocket)

  build-worker:
    name: Build and push model worker image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    outputs:
      image: ${{ steps.build.outputs.image }}
    steps:
      - uses: actions/checkout@v4

      - name: Build and push
        uses: nais/docker-build-push@v0
        id: build
        with:
          team: ao-ki-taskforce
          identity_provider: ${{ secrets.NAIS_WORKLOAD_IDENTITY_PROVIDER }}
          project_id: ${{ vars.NAIS_MANAGEMENT_PROJECT_ID }}
          build_args: REQUIREMENTS=requirements/worker-gpu.txt
          target: model-worker
          image_suffix: -worker

  deploy-dev:
    name: Deploy to dev
    needs: build-api
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        uses: nais/deploy/actions/deploy@v2
        env:
          CLUSTER: dev-gcp
          RESOURCE: .nais/dev.yaml
          VAR: image=${{ needs.build-api.outputs.image }}

  deploy-prod:
    name: Deploy to prod
    needs: [build-api, deploy-dev]
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        uses: nais/deploy/actions/deploy@v2
        env:
          CLUSTER: prod-gcp
          RESOURCE: .nais/prod.yaml
          VAR: image=${{ needs.build-api.outputs.image }}
